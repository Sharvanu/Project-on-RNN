# Project-on-RNN
This project demonstrates how to build and train a Recurrent Neural Network (RNN) to learn and predict patterns in sequential data. RNNs are powerful models for time-dependent tasks because they maintain a hidden state that remembers previous inputs.
