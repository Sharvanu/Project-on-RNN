# 🔁 Recurrent Neural Network (RNN) – Sequence Prediction Project

This project implements a **Simple Recurrent Neural Network (RNN)** to learn and predict sequential patterns.  
RNNs are ideal for modeling time-series and temporal data because they maintain memory through recurrent connections.

---

## 🚀 Features

- Generate or load sequential data (sine wave or custom file)
- Normalize and prepare data for training
- Convert sequences into sliding window inputs
- Build and train a SimpleRNN model using Keras
- Predict future values
- Plot the results (predictions vs. actual)
- Save & load trained RNN model
